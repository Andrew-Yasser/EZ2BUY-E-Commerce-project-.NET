@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access denied";
}

<header>
    <div class="container">
        <div class="border border-danger rounded mt-5 p-3 bg-light text-center">
            <span class="text-danger fs-5 fw-bold">@ViewData["Title"] : </span>
            <span class="text-danger">You do not have access to this resource.</span>
        </div>
            <div class="mt-3 text-center">
                <a class="btn btn-dark p-2" asp-area="Customer" asp-controller="Home" asp-action="Index">Back to Home</a>
            </div>
    </div>
</header>
